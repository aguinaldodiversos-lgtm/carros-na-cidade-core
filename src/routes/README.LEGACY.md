# `src/routes`

**Montado em `app.js`:** apenas `health.js` e `metrics.js`.

Rotas CommonJS antigas que duplicavam `src/modules/*` foram **removidas** para evitar divergência e confusão.

API oficial: `src/modules/*/routes.js` registradas em `src/app.js`. Mapa: `docs/api-routes-inventory.md`.
